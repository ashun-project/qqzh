doctype html 5
html
    head
        title=pageTitle
        meta(charset="utf-8")
        meta(name="keywords", content=pageKeyword)
        meta(name="description", content='#{pageDescrition}')
        meta(name="applicable-device", content="pc,mobile")
        meta(name="MobileOptimized", content="width")
        meta(name="HandheldFriendly", content="true")
        link(rel="stylesheet", type="text/css", href="/css/style.css")
    body
        .header !{headerHtml}
        .menu-child
            each item in meituMenus
                a(href="#{host+'/meitu/'+item.type}/", title="#{item.name}", class="#{item.type == type? 'active' : ''}") #{item.name}
        //- .banner
        //-     img(src="/img/bn.jpg", alt="banner_情趣综合平台")
        .search-result
            h1 当前位置：精品美图&nbsp;&nbsp;&nbsp;搜索字段：#{searchCont}&nbsp;&nbsp;&nbsp;共：#{searchTotal}条
        .lsit-items
            .data-list
                each item in listData
                    a(href="#{host+ '/meitu/' +type+'/'+item.id}", title="#{item.title}", target="_blank")
                        .list-img
                            img(src="#{host+item.img.replace('https://mtl.ttsqgs.com','/meitustatic')}", alt="#{item.title}")
                        .list-profile
                            p #{item.imglen}
                            p #{item.jigou}
                            p #{item.mutie}
                        .list-title
                            strong #{item.title}
                -if(!listData.length)
                    span.no-data 没有找到数据，请重新选择！
        .meitu-page !{page} 
        .footer
            span © 2011-2018 Umeng.com.All Rights Reserved